NODE_ENV=development
PORT=4000

# MySQL
DB_HOST=127.0.0.1
DB_PORT=3306
DB_USER=root
DB_PASSWORD=keerthiga@98356
DB_NAME=leavemanage

# JWT — use a long random string in production (32+ chars)
JWT_SECRET=4274184eba7c084add61f78ef44e1ba54837712f7e228454a9f0cb85cf668691
JWT_EXPIRES_IN=8h

# Email (optional — parent notifications log as skipped if unset)
SMTP_HOST=
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=
SMTP_PASS=
MAIL_FROM="Campus Leave <noreply@college.edu>"

# CORS — comma-separated origins, or * for dev (Socket.io uses the same list)
# Browsers with credentials: prefer explicit origins (not *) in production.
CORS_ORIGIN=http://localhost:5173

# Realtime: Socket.io shares PORT; client connects with JWT:
#   io(url, { auth: { token: '<JWT>' }, transports: ['websocket'] })
# Horizontal scaling: add @socket.io/redis-adapter + Redis pub/sub.
